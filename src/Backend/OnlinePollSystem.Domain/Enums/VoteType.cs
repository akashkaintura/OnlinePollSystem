namespace OnlinePollSystem.Domain.Enums
{
    public enum VoteType
    {
        Single = 0,      // User can vote for only one option
        Multiple = 1     // User can vote for multiple options
    }
}